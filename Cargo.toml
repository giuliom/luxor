[package]
name = "luxor"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.9"
chrono = "0.4"
tokio = { version = "1.52.3", features = ["full"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }